# ===dotfiles===
# Zsh-specific aliases and functions.
# Shared aliases/functions live in ~/.shell/aliases (sourced before this file).

function vimf() {
  if [[ "$#" -eq 0 ]]; then
    nvim "$(fzf)"
  else
    nvim "$(fzf -1 -q "$1")"
  fi
}
